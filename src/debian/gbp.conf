[DEFAULT]
# the default build command
builder=debuild -i\.git -I.git -us -uc -b
# the default branch for upstream sources
upstream-branch=master
# the default branch for the debian patch
debian-branch=master

